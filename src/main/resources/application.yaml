spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pay_mobile
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8085
---
#spring.main.allow-circular-references= true
#
#spring:
#  main:
#    allow-circular-references: true